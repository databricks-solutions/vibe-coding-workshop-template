# Workspace Instructions — Vibe-Coding Workshop

## Error Handling — MANDATORY

**On ANY error during the AppKit workshop, STOP and read `docs/troubleshooting_gc.md` FIRST.**

This file contains the complete error-to-fix lookup tables for all 5 workshop steps, pre-deploy checklists, post-deploy verification steps, and correct SDK patterns. Match the error message or symptom in the tables and apply the fix exactly as described. Do NOT improvise a workaround or hallucinate a fix before consulting this reference.

Common categories covered: MCP client setup, SDK file operations (base64 encoding, mkdirs, unicode in JSX), scaffold/build errors, deploy failures, Lakebase provisioning, resource binding types, plugin order, API route registration, and runtime debugging.

---

## Canonical Pattern Repository

The repo at `v2v-in-geniecode/vibe-coding-workshop-template/` is the canonical
pattern repository for this workspace. All reusable skills, prompts, and
framework code live there. Generated artifacts (Gold designs, pipelines,
app source, plans) are written to the repo root — never inside the framework
folders themselves.

---

## Three Main Folders

| Folder | When to Use |
|--------|------------|
| `apps_lakebase/` | Building or modifying **Databricks Apps** — scaffolding, plugins (analytics, Lakebase, Genie, Files), deployment, Lakebase wiring. Contains 6 skills covering the full AppKit lifecycle. |
| `data_product_accelerator/` | Building **data products** — medallion architecture, pipelines, semantic layer, monitoring, ML, GenAI agents. Contains 59 skills across 9 pipeline stages. |
| `agentic-framework/` | Designing and building **multi-agent systems** — PRD analysis, skill scaffolding, tool building, agent loops, testing, deployment. Contains 8 agent prompts + 1 Foundation Model skill. |

---

## Genie Code Behavior Rules

### Apps

When asked to build or modify an app, **prefer AppKit** — use the `@appkit-apps`
skill and the scaffolding patterns in `apps_lakebase/skills/`. Do not generate
raw FastAPI, Express, or pg-based apps from scratch. AppKit provides the correct
plugin architecture (`analytics()`, `lakebase()`, `genie()`, `server()`) and
handles Databricks auth, app.yaml, and deployment automatically.

If AppKit is explicitly inappropriate (e.g., the user needs a non-TypeScript
stack), say so and explain the tradeoff before proceeding with raw code.

### Data Products

When asked to build data products, **follow the 9-stage accelerator** and use
`SKILL.md` files in `data_product_accelerator/skills/` as primary guidance:

1. Gold Design → 2. Bronze → 3. Silver → 4. Gold Impl → 5. Planning →
6. Semantic Layer → 7. Observability → 8. ML → 9. GenAI Agents

Read `@data-product-accelerator` for the full skill map. Match the user's
keywords to the correct stage before writing any code. Start at Stage 1 for
new projects; jump to the relevant stage for incremental work.

### Agents

When asked about agents, **reuse patterns in `agentic-framework/` first** —
the 8 agent prompts and the Foundation Model agent-loop skill contain
battle-tested patterns for PRD analysis, tool building, multi-agent
orchestration, and deployment. Read `@agentic-framework` for the full pipeline.

Do not build agent loops from scratch when the `foundation-model-agent-loop`
skill already provides the correct Databricks Foundation Model tool-calling
pattern with structured output parsing.

---

## MCP Tooling

All Genie Code workflows that need deployment, Lakebase provisioning, or Genie
Space management should use the MCP AppKit Skill server. Read `@mcp-appkit-tooling`
for setup and the complete tool reference (11 tools).

MCP setup is always the first step. If the user hasn't connected yet, guide
them through `@mcp-appkit-tooling` before proceeding.

---

## Skill Routing (Quick Reference)

| User Says | Read This Skill First |
|-----------|----------------------|
| "build an app", "scaffold", "AppKit", "Lakebase" | `@appkit-apps` |
| "data product", "pipeline", "Gold layer", "Genie Space" | `@data-product-accelerator` |
| "build agent", "PRD", "tool-calling", "multi-agent" | `@agentic-framework` |
| "MCP setup", "deploy app", "OAuth", "tool reference" | `@mcp-appkit-tooling` |
| "where do I start", "workshop", "which track" | `@workshop-orchestrator` |

---

## Agent Memories

### Databricks Apps Build Environment (verified 2026-07-24)

- `npm` v10.9.2 and `node` v22.16.0 are available; **`npx` is NOT in PATH**
- **`@databricks/appkit` is a CLI-only tool** with commands: `setup`, `generate-types`, `lint`, `docs`, `plugin` — it has **no `build` or `start` commands**
- The `appkit_scaffold_app` MCP tool generates `package.json` and `app.yaml` with `npx @databricks/appkit <cmd>` — these MUST be overwritten
- Correct build script: `"build": "npm run typegen && vite build"` with `"typegen": "npm exec @databricks/appkit -- generate-types"` (not bare `vite build` — typegen generates Lakebase types and prevents build failures when Lakebase is wired in later)
- Correct start: `"start": "tsx app.ts"` with `tsx` as a dependency; `app.yaml` command: `["./node_modules/.bin/tsx", "app.ts"]`
- Vite config needs `root: "client"` (where `index.html` lives) and `outDir: "../dist"` (AppKit's `server()` serves from `dist/`)
- `client/index.html` is mandatory for vite build — without it: `Could not resolve entry module "index.html"`
- `app.ts` must only include `server()` plugin when no resources are configured — `analytics()`, `genie()`, `lakebase()`, `files()` crash at runtime without their respective env vars/resources
- `server/server.ts` must export `registerRoutes(appkit)` using `appkit.server.extend((app) => {...})` — NOT `server.extend((app, ctx))` or `createApp()`
- `app.ts` with Lakebase must use `onPluginsReady` pattern: `createApp({plugins:[lakebase(), server()], async onPluginsReady(appkit){await registerRoutes(appkit)}})` — NO `export default`, NO `autoStart: false`, NO `.then()`, NO `appkit.server.start()`, NO top-level `await` (CJS constraint)
- Lakebase queries use `appkit.lakebase.query(sql, params)` — NOT `ctx.lakebase.getPool()` or `createLakebasePool()`
- Health endpoint returns `{ data: [{ status: "connected" }], source: "live" }` — wrapped in `data` array for `useLakebaseData` hook compatibility
- **Deploy path (SDP only):** Use `validate_and_deploy()` from `apps_lakebase/gc-prompt-conversion/workshop-variables.md` — MCP `appkit_validate` plus SDK `w.apps.*`. **No** Databricks Jobs and **no** `_deploy_app` notebook for deployment.
- Product name: **StayFindr** (not VacaVista — the repo standard is StayFindr throughout)
- `appkit_add_lakebase` MCP tool does **NOT** create Lakebase projects or databases — it only returns boilerplate file snippets. Create projects first via SDK (`w.database` API), CLI (`databricks postgres create-project`), or Declarative Automation Bundles
- MCP server: `https://mcp-appkit-skill-4101016551133680.0.azure.databricksapps.com/mcp`
- OAuth scope: `v2v-gc-agent` (client_id, client_secret)

### MCP AppKit Skill — Tool Signatures (verified 2026-07-24)

The MCP server exposes **11 tools** with these names:

| # | Tool Name | Category |
|---|-----------|----------|
| 1 | `appkit_scaffold_app` | Scaffold |
| 2 | `appkit_add_lakebase` | Plugin |
| 3 | `appkit_add_genie_panel` | Plugin |
| 4 | `appkit_deploy` | Deploy |
| 5 | `appkit_add_analytics` | Plugin |
| 6 | `appkit_add_files_browser` | Plugin |
| 7 | `appkit_validate` | Validation |
| 8 | `appkit_list_apps` | App management |
| 9 | `appkit_get_app_status` | App management |
| 10 | `appkit_provision_lakebase` | Infrastructure |
| 11 | `appkit_manage_app_resources` | Infrastructure |

**Required parameters (updated 2026-07-24):**
- `appkit_scaffold_app`: `app_name` (string) + `description` (string) — both required
- `appkit_validate`: `app_name` (string) + `source_code_path` (string) — both required; old `workspace_path` no longer works
- `appkit_deploy`: `app_name` (string) + `source_code_path` (string) — both required; old `workspace_path` no longer works

**Old names that do NOT exist:** `generate_appkit_scaffold`, `deploy_appkit_app`, `setup_lakebase_project`, `get_lakebase_endpoint_status`, `optimize_lakebase_endpoint`, `grant_lakebase_role`, `get_app_deployment_status`, `list_genie_spaces`, `get_genie_space`, `appkit_add_trpc_route`, `appkit_get_app_logs`, `appkit_add_express_route`, `appkit_add_serving_endpoint`, `appkit_add_vector_search`, `appkit_push_files`

### Deploy Rules (verified 2026-07-24)

- **Always run `appkit_validate`** before deploying — it catches missing `app.yaml`, bad scripts, and misconfigured paths before the build fails
- **`appkit_validate` may false-negative** on individual file checks (`app.yaml not found`) when the MCP SP lacks `CAN_READ` on the source folder. Check for `permission_denied` warnings in the response. Fix by granting `CAN_READ` to the MCP SP using its `service_principal_client_id` (the UUID, not the numeric `service_principal_id`). See `01-appkit-scaffold/SKILL.md` Step 5 for the grant code
- **`appkit_deploy` runs as the MCP service principal**, not the current user. Two permissions must be granted first:
  1. **CAN_MANAGE on the app** — `w.permissions.set(request_object_type="apps", request_object_id=APP_NAME, ...)`
  2. **CAN_MANAGE on the workspace directory** — `w.permissions.set(request_object_type="directories", request_object_id=str(folder_status.object_id), ...)`
  - Both use `AccessControlRequest(service_principal_name=sp_client_id, permission_level=PermissionLevel.CAN_MANAGE)`
  - Without these: `"not authorized"` or `"error listing files: no files found"`
- **Primary method (Genie / workshop helpers):** After SP grants, use **`validate_and_deploy(APP_NAME, APP_BASE)`** from `apps_lakebase/gc-prompt-conversion/workshop-variables.md` — runs MCP `appkit_validate`, then SDK `create_and_wait` (if missing), `ensure_app_active`, and `deploy_and_wait` with `AppDeployment(source_code_path=APP_BASE)`. Do not hand-roll that sequence in prompts; the helper is the contract (see `gc-prompt-header.md`). This is the **only** supported deploy automation — no Jobs.
- **Optional — MCP `appkit_deploy`:** Direct MCP deploy (same SP identity as `appkit_validate`); requires the same directory/app permissions. Prefer `validate_and_deploy` in Genie so create/activate/deploy use session `w`.
- **`App` object quirk:** Use `app.compute_status.state` (not `app.status`) — the `App` object has no top-level `.status` attribute
- **`.state.name` for string comparison:** Use `app.compute_status.state.name` to get `"ACTIVE"` — `str(state)` returns `"ComputeState.ACTIVE"` which breaks `== "ACTIVE"` comparisons and causes infinite polling loops
- **Common deploy failure:** `error: unknown command 'build'` → means `package.json` still has `appkit build` instead of `npm run typegen && vite build`

### MCP Setup (verified 2026-07-24)

- **Kernel restart required** after `%pip install databricks-mcp` — the package pulls `pydantic` v2.13+ which needs `typing_extensions` ≥ 4.15 (`Sentinel`). Serverless has an older version. Must call `dbutils.library.restartPython()` before importing `DatabricksMCPClient`
- **`nest_asyncio` is required** — add `import nest_asyncio; nest_asyncio.apply()` before any `DatabricksMCPClient` calls. Databricks notebooks (and Genie Code) already have an active asyncio loop; `DatabricksMCPClient.list_tools()` calls `asyncio.run()` internally which crashes without `nest_asyncio`
- SDK file writes must use `format=ImportFormat.AUTO` (the enum from `databricks.sdk.service.workspace`), not the string `"AUTO"`
- **`service_principal_client_id` vs `service_principal_id`:** Permissions APIs and Lakebase role grants (`w.database.create_database_instance_role`) require the UUID (`app_info.service_principal_client_id`), NOT the numeric `service_principal_id`. Using the wrong one gives "Identity not found" errors
- **Lakebase resource type:** AppKit's `lakebase()` plugin requires a `postgres`-type resource (`AppResourcePostgres`), NOT a `database`-type resource (`AppResourceDatabase`). Using the wrong type causes `ConfigurationError: Missing required resources: postgres:Postgres [lakebase]` at startup. Discover database paths via `w.postgres.list_databases(parent=f"projects/{APP_NAME}/branches/production")`
